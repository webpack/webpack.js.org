sudo: false
language: node_js
node_js:
  - "14"

matrix:
  fast_finish: true

cache:
  yarn: true
  directories:
    - node_modules
    - dist
    - .cache

stages:
  - Build

jobs:
  fast_finish: true

  include:
    - stage: Build
      name: Lint and Build
      before_install: npm install --global yarn
      install: yarn
      script:
        - yarn lint:js || travis_terminate 1
