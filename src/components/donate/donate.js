// Import Dependencies
import React, { Component } from 'react'

// Import Components


// Load Styling
import './donate-style'

// Export the "Donate" component
export default class Donate extends Component {
    render() {
        return (
            <main className="donate">
                <span>Donate Content</span>
            </main>
        )
    }
}